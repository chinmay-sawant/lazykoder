
Your previous response was truncated due to length (max_tokens). Return a more
compact JSON envelope: keep at most 3 entries per array, shorten text and
evidence to one sentence each, and omit low-value recent_context. The
envelope must still be valid JSON with the six required keys.
